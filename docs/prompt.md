Using that `design system`as the strict visual specification and `base.html`and others as live reference implementations, build all remaining Django templates for the `{APP}` app — extending `base.html`, using Bootstrap 5 + Bootstrap Icons 1.11.3 + Chart.js 4 where needed, following every token, component pattern, spacing rule, and Django-specific constraint defined in the design system exactly, with no deviation in color, typography, or component structure.
